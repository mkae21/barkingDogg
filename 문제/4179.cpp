#include <bits/stdc++.h>

//연산 1억번 
using namespace std;

string maze[1001];
int jdist[1001][1001];
int fdist[1001][1001];

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int r,c;
    cin >> r >> c;

    //maze 초기화
    for(int i = 0 ; i < r ; i++)
    {
        cin >> maze[i];
        int j = 0;
        
        for(auto& c : maze[i])
        {
            
        }
    }



    queue<pair<int,int>> q;
    
    while(!q.empty())
    {
        
    }

    return 0;
}